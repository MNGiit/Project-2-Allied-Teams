<h1>Jobs</h1>
<h2>Jobs inside the database would go here.</h2>
<%= link_to "New Job", new_job_path %> <br>
<% if session[:user_id] %>
    <%= link_to "Log Out", logout_path, method:'delete' %>
    <% else %>
    <%= link_to "Log In", login_path %><br>
<% end %>

<!-- Filter -->
<!-- form_with url: jobs_search_path, method: :get do |form| --> <!-- can replace jobs_search_path with /jobs/search -->
<!-- Function -->
<!-- Department -->
<!-- Location -->
<!-- submit -->
<!-- end of filter -->

<% @jobs.each do |i| %>
  <p><%= link_to i.id, job_path(i) %>, new: <%= i.new_applicants %>, active: <%= i.active_applicants %>, hired: <%= i.hired_applicants %> %>
<% end %>
